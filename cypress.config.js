// cypress.config.js
module.exports = {
  e2e: {
    setupNodeEvents(on, config) {
      // You can add custom Node.js event listeners here if needed
    },
    baseUrl: 'https://www.google.com', // Optional: Set a base URL if needed
  },
};

